server:
  port: 8084
spring:
  cloud:
    config:
      server: 
        git: 
          default-label: config-server
          uri: https://github.com/GiovanniHernandezO/microservicios-tutorial
          search-paths: config-data
  application: 
    name: config-service